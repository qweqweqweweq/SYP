<Window x:Class="SYP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SYP"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Система управления персоналом" Height="650" Width="1000" MinHeight="550" MinWidth="900" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#FF1F2F47" Grid.Row="0">
            <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Система управления персоналом"
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontSize="14"
                       Margin="10,0" FontFamily="/Fonts/#Montserrat SemiBold"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Style="{StaticResource WindowControlButtonStyle}" Tag="Minimize" MouseLeftButtonDown="Minimize_Click">
                        <fa:ImageAwesome Icon="WindowMinimize" Width="16" Height="16" Foreground="White"/>
                    </Border>
                    <Border Style="{StaticResource WindowControlButtonStyle}" Tag="Maximize" MouseLeftButtonDown="Maximize_Click">
                        <fa:ImageAwesome Icon="WindowMaximize" Width="16" Height="16" Foreground="White"/>
                    </Border>
                    <Border Style="{StaticResource WindowControlButtonStyle}" Tag="Close" MouseLeftButtonDown="Close_Click">
                        <fa:ImageAwesome Icon="WindowClose" Width="16" Height="16" Foreground="White"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="243*"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>
            <Frame x:Name="frame" NavigationUIVisibility="Hidden" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>
</Window>

<Page x:Class="SYP.Pages.Add_Edit.DepartmentEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SYP.Pages.Add_Edit"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DepartmentEdit" FontFamily="/Fonts/#Montserrat">

    <Grid Background="#FFDCE4F1">
        <Border Background="White" CornerRadius="0,0,10,10" Height="60" VerticalAlignment="Top" Effect="{DynamicResource ShadowEffect}">
            <Grid>
                <Label x:Name="lbTitle" Content="Добавление нового отдела" FontSize="22" FontFamily="/Fonts/#Montserrat SemiBold" Foreground="#202a45" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
        <Border Background="White" CornerRadius="10" Margin="20,140,20,120" Padding="10" Effect="{DynamicResource ShadowEffect}">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Наименование отдела:" FontSize="15" Grid.Row="0" Grid.Column="0" Foreground="#202a45"/>
                <TextBox x:Name="NameDepartment" FontSize="15" Height="30" Grid.Row="0" Grid.Column="1" Style="{StaticResource RoundedTextBoxEdit}" Foreground="#202a45" VerticalContentAlignment="Center"/>
            </Grid>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,20">
            <Button Content="Отмена" Style="{StaticResource CancelButtonStyle}" Margin="0,0,10,0"/>
            <Button x:Name="btnText" Content="Сохранить" Style="{StaticResource EditButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
